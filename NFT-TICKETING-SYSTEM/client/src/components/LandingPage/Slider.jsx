import { Box, Container } from "@mui/material";
import React from "react";

const Slider = () => {
  return (
    <Box>
      <Container maxWidth="xl">slider</Container>
    </Box>
  );
};

export default Slider;
