import _extends from "@babel/runtime/helpers/esm/extends";
import React from 'react';
import { render } from '@testing-library/react';
import { Select } from '../';
import { mockRef } from '../../test/utils';

var makeSelectFixture = function makeSelectFixture() {
  var props = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  return /*#__PURE__*/React.createElement(Select, _extends({
    "data-testid": "select"
  }, props));
};

makeSelectFixture.displayName = "makeSelectFixture";
describe('Select', function () {
  it('should forward ref to underlying <select />', function () {
    var ref = mockRef();
    render(makeSelectFixture({
      ref: ref
    }));
    expect(ref.current).toBeInstanceOf(HTMLSelectElement);
  });
});