"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _constants = require("../../../constants");

var _mimeTypesToExtensions = _interopRequireDefault(require("../../src/utils/mime-types-to-extensions"));

describe('mimeTypesToExtensions', function () {
  it('should return extensions based on mimeTypes', function () {
    var mimeTypes = Object.values(_constants.MimeType);
    var expected = Object.keys(_constants.MimeType).map(function (key) {
      return ".".concat(key);
    });
    var result = (0, _mimeTypesToExtensions["default"])(mimeTypes);
    expect(result).toStrictEqual(expected);
  });
  it('should discard mimeTypes not found', function () {
    var badMimeType = 'does-not/exist';
    var expected = [];
    var result = (0, _mimeTypesToExtensions["default"])([badMimeType]);
    expect(result).toStrictEqual(expected);
  });
});