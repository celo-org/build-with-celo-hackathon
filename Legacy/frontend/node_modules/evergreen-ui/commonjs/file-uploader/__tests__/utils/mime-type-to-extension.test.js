"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _constants = require("../../../constants");

var _mimeTypeToExtension = _interopRequireDefault(require("../../src/utils/mime-type-to-extension"));

describe('mimeTypeToExtension', function () {
  it.each(Object.entries(_constants.MimeType))('should return .%s when mimeType is %p', function (key, mimeType) {
    var result = (0, _mimeTypeToExtension["default"])(mimeType);
    expect(result).toBe(".".concat(key));
  });
  it('should return undefined when mimeType cannot be found', function () {
    var badMimeType = 'does-not/exist';
    var result = (0, _mimeTypeToExtension["default"])(badMimeType);
    expect(result).toBeUndefined();
  });
});