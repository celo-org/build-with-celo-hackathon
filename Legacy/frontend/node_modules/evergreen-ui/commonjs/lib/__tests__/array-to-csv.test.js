"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _faker = require("@faker-js/faker");

var _arrayToCsv = _interopRequireDefault(require("../array-to-csv"));

describe('arrayToCsv', function () {
  it.each([undefined, null, '', []])('should return an empty string when value is %p', function (value) {
    var result = (0, _arrayToCsv["default"])(value);
    expect(result).toBe('');
  });
  it('should return comma separated string of values', function () {
    var values = _faker.faker.random.words(10).split(' ');

    var result = (0, _arrayToCsv["default"])(values);
    expect(result).toStrictEqual(values.join(','));
  });
});